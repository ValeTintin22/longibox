import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.longibox.app',
  appName: 'longibox',
  webDir: 'dist/angular-shopping-app/browser'
};

export default config;
